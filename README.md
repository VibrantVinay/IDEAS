--- a/README.md
+++ b/README.md
@@ -6,15 +6,13 @@
 
 This contains everything you need to run your app locally.
 
-View your app in AI Studio: https://ai.studio/apps/drive/15aykOwdzvUdTpVVi59X6gMGnhF8VdzM3
-
 ## Run Locally
 
 **Prerequisites:** Node.js
 
 
 1. Install dependencies:
-   `npm install`
-2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
-3. Run the app:
+    `npm install`
+2. Run the app:
    `npm run dev`
